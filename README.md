# 星之卡比图鉴

这是一个面向群聊的 AstrBot 盟友抽取与收藏图鉴插件。它把每个盟友保存为“固定编号 + 名字 + 素材图片”，适合拥有数百张、并且还会继续增加的素材库。

项目仓库：[Whereis-Alice/astrbot_plugin_kirby_catalog](https://github.com/Whereis-Alice/astrbot_plugin_kirby_catalog)

## 主要功能

- 每日抽取盟友，保留原插件的每日次数和连续未出新保底逻辑。
- 自动继承旧插件的群组记录、个人解锁记录、抽取次数和图鉴编号。
- 群图鉴和个人图鉴都显示编号与名字；群图鉴会将未解锁素材显示为灰度图。
- 管理员可以引用 Bot 发出的盟友消息替换素材图片，历史记录不受影响。
- 管理员可以改名或手动添加盟友；改名会同步所有用户的当前记录、解锁记录和后续图鉴。
- 支持“猜盟友”小游戏，答对、猜错或超时都会揭晓答案；猜盟友不会改变任何人的图鉴。
- 猜盟友时可以直接引用题目图片并发送名字作答；一轮进行中不会被其他用户覆盖。
- 支持随机查看盟友，不会增加任何用户的抽取、当前盟友或解锁记录。
- 支持群内盟友解锁排行榜和按关键词查询盟友名单。
- 支持通过 `卡比百科` 查询 WiKirby 页面摘要、信息框、出现地点、趣闻、首图和来源链接；模板生成的出现地点表会提取实际标记为出现的关卡。
- 支持通过 `卡比百科名称` 只查询角色在各语言中的官方名称，例如简体中文、繁体中文和日语名称。
- 可以一次性清理改名前遗留的旧名称，并同步用户历史记录。
- 可以按编号把名称不同的重复条目明确合并到正确条目。
- 不包含原插件中与收藏图鉴无关的夺取机制，旧的相关状态文件也不会被读取。

## 安装

1. 将 `astrbot_plugin_kirby_catalog` 文件夹放入 AstrBot 的插件目录。
2. 安装依赖：

   ```powershell
   python -m pip install -r requirements.txt
   ```

3. 停用旧的 `astrbot_plugin_AnimeWife` 插件，再重启 AstrBot。
4. 新插件第一次启动时，会自动扫描标准旧数据目录：

   ```text
   data/plugin_data/astrbot_plugin_AnimeWife
   ```

   新插件的数据会写入独立目录：

   ```text
   data/plugin_data/astrbot_plugin_kirby_catalog
   ```

   这里的 `data` 是 AstrBot 的数据目录；例如 AstrBot 安装在 `/root/AstrBot` 时，完整路径就是
   `/root/AstrBot/data/plugin_data/astrbot_plugin_kirby_catalog`。

   两个插件不会共用写入目录，因此不会因为插件标识相同而互相覆盖。

### 图片素材目录

新素材放在：

```text
data/plugin_data/astrbot_plugin_kirby_catalog/img/allies
```

首次启动或执行迁移命令时，旧目录中的本地素材会复制到新目录；原目录不会被删除：

```text
data/plugin_data/astrbot_plugin_AnimeWife/img/wife
```

迁移完成后，新插件优先使用自己的素材目录，不再依赖旧插件文件。旧目录仍会作为迁移失败时的只读兼容来源。确认新目录素材完整后即可删除旧插件及旧数据。

如果旧插件数据不在标准路径，可以在 AstrBot 插件配置中填写 `legacy_data_dir`，或使用管理员命令 `星之卡比图鉴迁移` 重新扫描并补齐缺失文件。

## 普通用户命令

| 命令 | 作用 |
| --- | --- |
| `今日盟友` | 抽取今天的盟友。直接发送这四个字即可，不需要 `/`；别名：`抽盟友`、`抽取盟友` |
| `查盟友` | 查看自己今天的盟友。可以追加成员编号、昵称，或直接 @ 成员 |
| `我的盟友图鉴` | 查看个人已解锁图鉴。别名：`盟友图鉴` |
| `星之卡比图鉴` | 查看本群完整图鉴，包含编号、名字和解锁状态。别名：`群盟友图鉴` |
| `随机盟友` | 随机查看一位盟友，不计入抽取次数或用户图鉴。别名：`随机查看盟友` |
| `猜盟友` | 发起一轮猜名；可回复 `猜盟友 名字`，也可引用题目图片直接发送名字。猜中只公布答案，不会收入个人图鉴 |
| `盟友排行榜` | 查看本群解锁数量前十名。别名：`星之卡比排行榜`、`图鉴排行榜` |
| `盟友名单 [关键词]` | 查看图鉴编号和名字，素材很多时可用关键词筛选 |
| `卡比百科 [角色名]` | 查询 WiKirby 页面简介、资料栏目、首图和来源链接。也可以引用盟友消息，按图鉴名称查询 |
| `卡比百科名称 [角色名]` | 只查询 WiKirby 的多语言官方名称，不返回百科简介。别名：`卡比百科名`、`卡比百科译名`；该能力也注册为 LLM 工具，名称为 `kirby_catalog_lookup_official_names` |
| `星之卡比图鉴帮助` | 查看命令速查。别名：`盟友帮助` |

`卡比百科` 不要求使用 `/`。例如：

```text
卡比百科 Driblee
卡比百科名称 Driblee
卡比百科 名称 Meta Knight
```

如果查询中文名后存在多个相关页面，插件会列出候选页面，避免把角色、关卡或续作页面混淆。WiKirby 查询失败时不会影响抽盟友、猜盟友和图鉴数据。

## 管理员命令

管理员权限由 AstrBot 的管理员配置控制。

### 替换错误素材

引用 Bot 发出的盟友图片消息执行：

```text
星之卡比图鉴换图 12
```

也可以省略编号，让插件从被引用消息中的 `#12` 或 `编号：12` 自动识别：

```text
星之卡比图鉴换图
```

引用消息需要包含图片。替换只改变素材文件，不会删除任何用户的解锁记录；个人图鉴下次生成时会自动使用新图片。

### 修改名字

```text
星之卡比图鉴改名 12 新名字
星之卡比图鉴改名 12 新名字 | 新作品来源
```

也可以直接引用 Bot 发出的盟友消息，只填写新名字：

```text
星之卡比图鉴改名 新名字
```

插件会从被引用消息中的 `#编号` 自动识别盟友。例如引用包含 `#12 星之卡比` 的消息后，发送
`星之卡比图鉴改名 结晶化天鹅罗利那`，就会修改编号 12。这个用法同样会同步所有用户的当前盟友、解锁记录以及后续图鉴；该命令仅限管理员使用。

插件会更新图鉴目录、素材文件名、当前盟友、所有个人解锁记录以及后续生成的群图鉴和个人图鉴。素材文件名中的来源前缀会保留，例如 `星之卡比 探索发现.旧名字.png` 会改成 `星之卡比 探索发现.新名字.png`；固定图鉴编号保持不变。

旧插件数据如果存在重复编号，插件会自动保留其中一个条目的原编号，并为其他条目分配新的未占用编号。用户收藏按素材文件名关联，不会因为编号修复丢失。

### 添加新盟友

引用一张图片执行：

```text
星之卡比图鉴添加 新盟友
星之卡比图鉴添加 新盟友 | 作品来源
```

插件会分配新的图鉴编号。添加不会直接把盟友发给某一位用户，之后可以通过抽取或猜名获得。

### 重新迁移

```text
星之卡比图鉴迁移
```

这个命令适合管理员在补回旧数据或调整旧目录后使用。迁移是幂等的，重复执行不会重复增加解锁记录。

### 清理改名前遗留条目

如果旧版本已经把素材复制到新目录，但改名前的条目仍出现在图鉴中，可以使用：

```text
星之卡比图鉴清理旧名 水晶 结晶化 水晶针卡比
```

这条命令会把“水晶天鹅罗利那”合并到“结晶化天鹅罗利那”，并同步所有用户的当前盟友和历史解锁记录。最后一个参数表示需要保留的旧名称；本例会保留一个“水晶针卡比”，如果它存在重复条目，插件会合并为一个。找不到唯一对应新名称的条目不会被删除，而会在回复中列出。

### 明确合并重复编号

当重复条目已经改成了不同名称时，请明确写出“重复编号 正确编号”：

```text
星之卡比图鉴删除重复 421 406 413 134
```

这会将 `#421` 合并到 `#406`，将 `#413` 合并到 `#134`，并同步所有用户记录。也可以使用别名 `星之卡比图鉴合并重复`。

## 数据继承说明

旧插件的用户数据可能是早期数组格式，也可能已经是包含 `current`、`unlocked` 的 JSON 对象。本插件启动时会统一转换为新的盟友字段，并保留：

- 当前盟友和日期；
- 每位用户的全部解锁盟友及首次解锁日期；
- 用户昵称；
- 连续未出新计数；
- 每日抽取次数；
- 旧图鉴编号。
- 旧素材目录中的本地图片文件；

旧素材文件名中的“作品.名字.扩展名”信息也会自动用于初始名称显示。之后建议使用固定编号管理素材，不要直接在文件管理器中改名；确需改名时请使用管理员命令，以便同步历史数据。

## 常见问题

### 为什么替换图片后旧消息里的图片没有变化？

已经发送出去的聊天消息不会被插件修改；替换影响的是之后的查盟友、抽取和图鉴生成结果。

### 为什么图鉴里有灰色问号？

这表示历史记录或旧图鉴编号仍然存在，但对应素材文件暂时找不到。补回同名素材，或使用管理员的换图命令即可恢复。

### 平台不支持读取引用图片怎么办？

可以把图片直接作为当前命令的附件发送。如果平台既不能提供当前图片，也不能提供引用消息中的图片，插件无法取得素材字节，这是适配器能力限制。

### WiKirby 查询设置

WiKirby 查询默认开启。可以在插件配置的“WiKirby 查询设置”中调整：

- 是否启用 `卡比百科`；
- 普通百科查询是否发送页面首图；
- 是否显示页面资料、出现地点和趣闻栏目；
- 是否使用 AstrBot 原生文本模型将百科简介和页面资料翻译成简体中文，以及使用哪个 Provider；
- 请求超时时间、缓存时间和摘要长度。

AI 翻译默认关闭。开启“使用 LLM 将百科简介和页面资料翻译成简体中文”后，插件会优先使用配置中指定的 Provider；Provider 留空时使用当前聊天会话的模型。翻译只处理 WiKirby 页面资料，不会把查询内容交给模型执行工具；模型不可用时会自动显示原文。

插件只读取 WiKirby 的公开 MediaWiki API，不会登录、编辑或上传内容。查询结果会保留页面来源链接。

如果 WiKirby 的 `api.php` 被云服务器出口 IP 暂时或持续返回 403，插件会自动重试并切换到 `www.wikirby.com` 的 REST API；精确页面查询还会继续尝试静态 `action=raw` 页面。页面查询、中文搜索和名称表读取会尽量继续工作；这段时间查询可能稍慢，但不会影响其它图鉴功能。

如果云服务器对 WiKirby 的所有入口都返回 403，可以部署项目中的 `cloudflare_worker/wikirby_proxy.js` 作为可选中转。Worker 必须设置 `WIKIRBY_PROXY_TOKEN`，然后把 Worker 地址和密钥填入插件配置；详细步骤见 `cloudflare_worker/README.md`。中转仍可能被 WiKirby 拦截，部署后必须先用文档中的 `curl` 命令确认返回 200。

## 致谢与上游参考

本项目在数据格式兼容、抽取流程和基础图鉴思路上参考并改造自：

1. [zgojin/astrbot_plugin_AW](https://github.com/zgojin/astrbot_plugin_AW)：原始 AstrBot 群老婆插件，上游代码与数据格式参考来源。
2. [Rinco304/AnimeWife](https://github.com/Rinco304/AnimeWife)：早期功能灵感来源。
3. [WiKirby](https://wikirby.com/wiki/Kirby_Wiki)：`卡比百科` 功能的资料来源和 MediaWiki API 服务。

感谢上游作者及贡献者的开源工作。WiKirby 页面内容除另有说明外采用 GNU Free Documentation License 1.3 或更高版本；本插件仅返回摘要、名称和来源链接，不重新打包 WiKirby 页面或图片。素材图片的版权和使用许可仍归原素材提供者所有。本项目使用 MIT License。
