# 更新日志

## v3.5.8 - 2026-08-10

### Kirby Fandom Worker 失败中转

- Kirby Fandom 查询现支持可选 Cloudflare Worker 兜底。它默认先直连，遇到 HTTP 403、429、可重试服务器错误或网络失败才通过中转重试；一次中转成功后会短暂优先走中转，避免同一次查询反复直连失败。
- Fandom 的 API 请求和页面首图下载均支持中转。新增 `fandom_proxy_url`、`fandom_proxy_token`；两项留空时自动复用已有的 WiKirby Worker 地址和密钥，也支持填写独立 Worker。
- `cloudflare_worker/wikirby_proxy.js` 升级为兼容 WiKirby 与 Fandom 的严格白名单中转。旧 WiKirby 请求保持兼容，现有 Worker 直接替换代码即可，不必新建 Worker 或更换 `WIKIRBY_PROXY_TOKEN`。

## v3.5.7 - 2026-08-08

### 爱丽丝自己的盟友图鉴

- 新增群命令 `看爱丽丝盟友图鉴`，并提供 `爱丽丝盟友图鉴`、`看爱丽丝图鉴`、`Bot盟友图鉴` 别名。它只展示 Bot 在当前群解锁的盟友，不会混入群友或其他群的数据。
- 新增 LLM 工具 `kirby_catalog_view_bot_gallery`。爱丽丝调用后会先把自己的当前群图鉴主动发到群里，再收到同一批轻量化图鉴页作为视觉输入，可看完后自然回应。
- 该工具不会抽盟友、不消耗每日次数、不修改图鉴；`bot_draw_llm_vision_enabled` 现同时控制 Bot 抽取素材和 Bot 图鉴页是否提供给视觉 LLM。

## v3.5.6 - 2026-08-07

### 未来任务自动抽盟友

- `kirby_catalog_draw_bot_ally` 现兼容 AstrBot Active Agent“未来任务”。当任务的“投递到”选择群聊时，会从该群会话读取群号并主动发送抽取文案、简介和素材图片。
- 定时任务会从当前 OneBot/NapCat 连接识别真实 Bot QQ 号，因此与手动 `Bot今日盟友` 共用同一份 Bot 图鉴和当天三次机会，不会意外创建 `bot_astrbot` 的第二套记录。
- 没有配置群聊投递目标的定时任务会保持拒绝；多账号或特殊连接方式无法自动识别时，可用新增的 `bot_draw_identity` 明确指定 Bot QQ 号。

## v3.5.5 - 2026-08-07

### Bot 视觉回应

- `kirby_catalog_draw_bot_ally` 在主动发送群内抽取消息后，会把完整文案和轻量化素材图作为工具结果提供给支持视觉的 LLM，让爱丽丝能实际查看同一张图后再自然回应。
- 新增 `bot_draw_llm_vision_enabled`，默认开启；关闭后工具仍会发群消息并提供完整文字，但不会把图片送入模型上下文。
- 视觉输入最高限制为 `1280 x 1280`、`1.5MP` 和 `2MiB`，避免大图拖慢模型调用或占用过多上下文。

## v3.5.4 - 2026-08-07

### Bot 主动抽取

- `Bot今日盟友` 与 `kirby_catalog_draw_bot_ally` 现在均使用 Bot 在当前群的独立每日次数，默认 3 次，并与 `daily_draw_limit` 共用配置值。
- LLM 调用 Bot 抽取工具后会主动向当前群发送完整抽取结果、已启用的简介和素材图片；工具返回值只用于提示 LLM 不要再次复述结果。
- Bot 的抽取次数、当前盟友和解锁记录继续与群友完全隔离，不会占用任何群友的次数或个人图鉴。

## v3.5.3 - 2026-08-07

### 今日盟友性能

- `今日盟友`、`随机盟友`、查盟友、图鉴进度和群图鉴不再在每次请求时全量扫描素材目录与所有群数据；插件启动、WebUI/管理员修改时仍会同步目录。
- 抽取池现在按图鉴版本缓存。日常抽取不再逐个检查全部素材文件，只有启动、素材增删、改名、恢复或管理员刷新后才重建。
- 发送缓存清理改为按时间间隔执行，默认最多每 5 分钟扫描一次，避免每条盟友消息遍历缓存目录。
- `今日盟友` 日志新增抽取、素材定位、图片准备、直发与总耗时分段，便于区分磁盘、图片处理和 NapCat 发送瓶颈。

### 图片发送配置拆分

- 新增独立的 `ally_media_*` 盟友素材图片保护配置。今日盟友、随机盟友、查盟友、猜盟友和图鉴默认回到 `2160px`、`8000px`、`18MP`、`8MiB` 的轻量安全阈值。
- WiKirby、Kirby Fandom 和简介卡片继续单独使用 `wiki_card_*` 高规格阈值；提高百科卡片高度或总像素不会再连带放大盟友素材，减少图片处理和 NapCat 上传等待。
- 原 `media_max_*` 通用配置已废弃。升级后请在 WebUI 分别调整新的“盟友素材”和“百科卡片”配置；旧的高值不会自动套用到今日盟友。

## v3.5.2 - 2026-08-05

### 百科卡片分页调节

- 将 `wiki_card_page_line_budget` 的可配置上限从 `300` 提高到 `3000`，默认值仍为 `110`。
- WebUI 配置说明现在明确推荐复杂页面从 `500` 或 `600` 开始测试，并说明高度、像素或文件大小超限时仍会自动降低预算重新分页。
- README 补充行预算范围以及它与 NapCat 图片安全阈值之间的关系。

## v3.5.1 - 2026-08-05

### NapCat 合并转发容错

- 超长百科的文字节点与卡片图片改为分批发送；普通短内容仍保持一条合并转发，图片批次默认每条最多 2 张。
- aiocqhttp/NapCat 默认由插件直接调用 OneBot 合并转发接口，因此可以在 `UploadForwardMsgV2` 或 `resId` 错误发生的位置捕获异常，不再把同一条失败消息交回 AstrBot 响应阶段。
- 多节点上传失败时自动二分为更小批次；单节点重试后仍失败时，文字改发普通消息，图片使用现有本地文件直发与 AstrBot 标准发送兜底。
- 合并转发节点内的图片现在同样经过尺寸、像素和文件大小保护；本地图片可使用 `file://` 路径，避免全部强制转为 Base64 后再交给 NapCat。
- `查看简介` 的合并转发接入同一发送与兜底链路。

### 配置与验证

- 新增 `forward_max_images_per_message`、`forward_direct_send_enabled`、`forward_retry_count`、`forward_retry_delay_seconds` 和 `forward_batch_delay_seconds`。
- 增加长文本与图片分离、图片限流、短转发合并、本地文件转发、失败批次缩小及普通消息兜底测试。

## v3.5.0 - 2026-08-05

### 超长百科与图鉴分页

- WiKirby 与 Kirby Fandom 的普通页面继续保持单张卡片；只有预计内容或渲染实测超过安全阈值时，才按简介续篇、资料栏目、语录条目和招式表行做语义分页。
- 分页保留全部正文、语录和招式行，续页重复页码、来源与招式表头；渲染后会记录宽高、总像素、文件大小和触发的阈值，超限时自动降低单页预算重试。
- 群图鉴和个人图鉴新增高度保护，只有超长图鉴才分页；相同素材、解锁状态和版式会复用生成缓存，避免重复处理上千张素材。

### NapCat 图片发送优化

- 新增可配置的 OneBot 本地 `file://` 直发路径，在 aiocqhttp/NapCat 上绕过 AstrBot v4.26.8 对图片的强制 base64 转换；失败会重试并自动回退 AstrBot 标准发送。
- NapCat 直发仅对 aiocqhttp 事件启用，并按源文件指纹复用共享目录暂存副本，重复查看不再反复复制或转码。
- 支持 AstrBot 与 NapCat 相同路径或不同容器挂载路径的共享目录映射，并定期清理插件自己的暂存文件。
- 今日盟友、随机盟友、查盟友、猜盟友、百科卡片和图鉴共用最终图片保护；超限时只生成可缓存的等比缩放发送副本，不改动原素材。
- 新增 JPEG RGB/JFIF 标准化、百科卡片 JPEG/PNG、清晰度、质量、宽高、总像素和文件大小配置。默认改为高清 JPEG，兼顾文字清晰度与 QQNT 上传稳定性。
- 默认安全阈值为宽 `2160px`、高 `8000px`、`18MP`、`8MiB`；这些数值是调用方兜底策略，不声明为 QQ 官方硬限制。

### Bot 独立抽取

- 新增 `Bot今日盟友`、`机器人今日盟友` 和 `Bot抽盟友`，Bot 使用按账号区分的独立持久化身份，不占用群友次数或个人图鉴。
- 新增 LLM 工具 `kirby_catalog_draw_bot_ally`。同一群当天重复调用保持幂等，只返回当天已抽结果，避免 Agent 重试造成多次抽取。
- Bot 解锁计入群汇总图鉴，默认不进入排行榜；显示名称、文案、功能开关和排行榜行为均可配置。

### 文档与验证

- README 新增超长卡片、图鉴分页、共享目录、NapCat 直发回退、`rich media transfer failed` 排查和 Bot 抽取说明。
- 新增百科语义分页完整性、普通页面单卡、图鉴按需分页与缓存、Bot 幂等持久化、OneBot 文件 URI 及失败回退测试。

## v3.4.0 - 2026-08-05

### 图鉴 WebUI 管理台

- 新增 AstrBot Dashboard 插件 Page，提供概览、素材库、群数据、回收站和操作记录五个管理模块。
- 素材库支持分页搜索、作品/类型/状态筛选、排序、详情编辑、拖放新增、换图和删除；名称或首次登场作品变化时同步重命名素材文件及所有群引用。
- 群数据支持查看全部群组和成员，编辑昵称、当前盟友、当前日期、连续未解锁次数、今日已用次数及额外次数，并可增删个人解锁、重置群内今日次数或删除成员数据。
- 删除素材改为移入回收站，保留固定图鉴编号、素材、简介覆盖和受影响用户快照；恢复时尽可能还原所有群引用，新增素材不会复用回收站中的编号。
- 新增 Dashboard 操作审计，以及跟随 AstrBot、卡比配色和暗色三种主题；主题偏好按 Dashboard 用户保存。

### 数据安全与兼容

- WebUI 写操作与群聊抽取共用同一把异步锁，避免管理修改和抽取同时落盘。
- 素材名称、首次登场作品与简介作为一次事务提交；成员资料、当前盟友和两类今日计数同样原子保存，任一文件写入失败会恢复修改前状态。
- 新增、删除、恢复和换图均使用原子写入；动态缩略图通过本地 WebP data URI 返回，不暴露服务器文件路径，也不依赖外部图床。
- Page Bridge 同时兼容 `onContextChange` 与 AstrBot v4.26.8 的 `onContext` 主题事件；完整管理台要求 AstrBot v4.26.8 或更高版本，旧版 AstrBot 上群聊命令仍可继续运行。
- WebUI 注册失败会记录日志但不会阻断插件核心消息功能。

### 界面与验证

- 桌面端采用高密度表格和右侧编辑抽屉，手机端自动切换为纵向列表与全屏编辑面板；本地打包 Lucide 图标，不依赖 CDN。
- 增加 Web API 路由、插件初始化、事务回滚、Page 资源和响应式主题测试，并完成桌面与手机视口视觉验收。
- 139 项单元测试与 9 个子测试通过；Python 编译、Ruff 关键错误规则、Ruff 格式检查和 `node --check` 通过。

## v3.3.2 - 2026-08-05

### NapCat 合并转发兼容

- 修复百科全文和图片被放进单个超大转发节点的问题；文字现在按段落拆分，图片或百科卡片使用独立节点。
- 新增 `forward_node_max_chars`，单个文字节点默认最多 3000 个字符；拆分只改变发送结构，不截断 WiKirby、Kirby Fandom 或查看简介的正文。
- 新增 `forward_max_nodes_per_message`，单条合并转发默认最多 20 个节点；超过后自动拆成多条合并转发，避开 NapCat 在节点过多时可能无法取得 `resId` 的问题。
- `查看简介` 的合并转发复用相同的拆分与分批逻辑。

### 查询速度与诊断

- 相同来源、相同 Provider 和相同原文的 LLM 翻译结果会在插件进程内缓存，缓存时长复用对应百科的 TTL 配置，减少重复查询等待和模型调用。
- WiKirby 与 Kirby Fandom 完整查询新增生成耗时日志，记录查询词、回复形式、正文字符数、转发节点数和生成耗时，便于区分百科抓取/翻译/卡片渲染慢与 NapCat 发送慢。
- 翻译缓存最多保留 128 项，并在插件卸载或重载时清理。

### 验证

- 增加长文本完整拆分、图片独立节点、超量节点分批和重复翻译缓存测试。

## v3.3.1 - 2026-08-05

### 简介长度与独立查看

- 新增 `ally_description_max_chars`，默认最多显示 600 个字符；超长简介以 `... ...` 结尾，设为 `0` 时不截断。
- 新增普通用户命令 `查看简介` / `查看盟友简介`，支持引用 Bot 发出的盟友消息，也可直接填写图鉴编号或完整名称。
- `查看简介` 不受自动简介开关影响；关闭 `今日盟友`、`随机盟友` 和 `查盟友` 的简介后仍可按需查看。

### 回复形式与卡片

- 新增普通消息、合并转发和单张简介卡片三种回复形式，长简介可避免直接刷屏。
- 简介卡片复用高分辨率 HTML 百科模板，合理排版盟友名称、图鉴编号、首次登场、简介和资料来源，并支持四套现有主题。
- 简介卡片不重复附带盟友素材图；HTML 渲染失败时自动回退到普通消息。

### 验证

- 增加长度边界、无限制模式、关闭自动简介后引用查看、合并转发、卡片 payload 和渲染失败回退测试。

## v3.3.0 - 2026-08-05

### 图鉴简介资料

- 为 1353 项规范图鉴建立按稳定 `entry_key` 匹配的资料库，包含角色与首次登场作品的中英文名称、简体中文简介、WiKirby 来源页面、修订号和时间戳。
- 简介从 WiKirby 页面开头的引语与导语提取，经简体中文翻译和术语规范化；已核验专名使用“官方中文（官方英文）”，没有可靠官方中文时保留英文。
- 能力、EX、结晶化、元素版和阶段版等独立条目保留各自简介入口，并在共用基础页面导语前增加简短的变体身份说明。
- 新增离线生成器 `tools/build_catalog_profiles.py`，支持页面缓存、缺页补取、Google/Bing 翻译、专名保护、断点续跑，以及完整和未匹配 CSV 报告。
- 翻译服务改动长文本分隔标记时自动按引语、署名和导语段落降级重试；生成后检测残留英文叙述并逐段补译，同时保留没有官方中文的英文专名。
- 增加 `THIRD_PARTY_NOTICES.md`、资源说明和 GFDL 1.3 许可副本，逐条资料保留可追溯来源。

### 消息与配置

- `今日盟友`、`随机盟友` 和 `查盟友` 按“基础信息、简介、百科提示、素材图片”的顺序发送；其他命令不会附带简介。
- 新增简介与百科提示独立开关、自定义提示文案、统一详情编排模板，以及随机盟友和查盟友基础文案模板。
- 图片继续作为消息链最后一个组件发送；资料缺失时自动省略简介，不影响抽取、随机查看或查询记录。
- 猜盟友额外读取资料库中的规范中英文名，继续支持中文或英文作答。

### 管理员维护

- 新增 `星之卡比图鉴简介 <编号> [新简介]`，可查看、新增或修改简介；支持引用 Bot 的盟友消息后省略编号。
- 新增 `星之卡比图鉴恢复简介 <编号>`，删除人工覆盖并恢复内置简介，同样支持引用消息。
- 人工简介独立保存在 `config/description_overrides.json`，改名后继续跟随稳定条目，插件更新和规范迁移会原样保留。
- 手动新增素材会获得稳定的 `manual:` 类型 `entry_key`，可安全关联人工简介。

### 验证

- 增加内置资料加载、官方英文名检索、人工覆盖持久化、改名后关联、恢复内置简介、详情模板、显示开关和引用编辑测试。
- 115 项单元测试通过；1353 项资料审计结果为缺失 0、未匹配 0、占位符残留 0、乱码 0、疑似英文正文 0。

## v3.2.0 - 2026-08-05

### 百科引用与猜名

- `卡比百科`、`卡比百科名称` 和 `卡比F` 引用盟友消息时优先使用图鉴中的英文 `page_title`；无编号时可从中英双语名称或素材文件名提取英文，并跳过开头的作品前缀。
- 修正引用文本读取范围：只解析被引用消息，不再把当前命令正文混入百科查询词。
- 猜盟友同时接受规范中文名和英文名，英文比较忽略大小写、空格和常见标点；EX、能力与形态仍使用各自的英文变体名，不会把基础页面名误判为正确答案。

### 抽取与管理

- 默认抽取文案调整为“盟友名称、图鉴编号、首次登场作品、今日剩余次数”的顺序，并新增可换行编辑的 `draw_message_template`。
- 新增管理员命令 `重置今日群抽取次数`，只清空当前群当天的已用次数、额外机会和冷却状态，不影响其他群或历史日期。
- 新增管理员命令 `增加今日抽取次数 @群友 [次数]`，按群、用户和日期增加可用机会；支持直接填写用户 ID，默认增加 1 次。
- 额外机会独立保存在 `draw_bonuses.json`，刷新、改名和后续规范迁移会将其视为非群配置原样保留。

### 验证

- 增加百科引用英文提取、作品前缀排除、中英文猜名、EX 名称隔离、自定义抽取文案、额外机会生效、跨群与跨日期重置，以及迁移保留额外次数的回归测试。

## v3.1.0 - 2026-08-05

### 独立能力与形态图鉴

- 新增可重复传入的 `--supplemental-assets` 迁移参数，将 1078 个基础角色与 275 个能力、形态、EX 和阶段条目合并为 1353 项完整图鉴。
- 每个补充条目使用独立 `entry_key`；同一 WiKirby 页面下的普通版、EX、元素形态、服装形态和阶段形态不再互相覆盖，个人进度与排行榜分别计数。
- 补充素材包含 75 种普通复制能力，以及进化能力、超级能力、塞满嘴、机器人装甲模式、64 组合能力、毛线变身、盟友能力、卡比猎人职业、结晶化和其他经过核验的变体。
- 图鉴排序继续以最早登场作品年份和作品发行顺序为主，同一作品内再按条目类型、页面标题和变体标识稳定排列。

### 旧数据恢复

- 新增可重复传入的 `--history-source`：服务器已经执行过 v3.0.x 时，可从迁移前 `.before-v3-*` 备份补回曾被合并的能力、形态和 EX 解锁，同时保留活动目录在迁移后新增的抽取记录。
- 增加上次迁移基线重建：利用 `migration_state.json` 与 `migration_plan.json` 剥离 v3.0.x 产生的错误归并，再合并正确历史和迁移后新增记录，避免简单并集把全服有效解锁从 `6351` 虚增为 `6889`。
- 历史合并以活动用户字段为主，昵称、累计次数、连续未出新计数和未知字段不会回退；未被用户改动的当前盟友按新规则纠正，迁移后主动更换的当前盟友继续保留。
- 历史数据源按只读目录处理，正式替换仅作用于 `--plugin-data`；重叠路径和位于数据或素材目录内的报告路径会在执行前被拒绝。
- 二次迁移优先使用现有 catalog 的稳定 `entry_key`，修复“暗影卡比”被能力名称规则误排除以及素材图片更新后同一条目可能失配的问题。
- `apply_plan()` 现在会强制写入完整迁移报告，确保后续升级始终可以重建历史基线；对应报告缺失时会停止迁移，不会冒险重复计数。
- 覆盖表改为直接指向能力或变体的 `entry_key`，修复球体喽啪元素版与 EX、结晶化角色、魔法洛亚造型、Boss 阶段等旧素材被并入普通版的问题。
- 修复覆盖目标解析：优先匹配 `entry_key` 和 `variant_key`，避免补充变体与基础页面同名时产生歧义。
- 移除“任意瓦豆鲁迪后缀”和“球体喽啪字样”自动归并规则；未知形态会进入复核报告，不再静默合并。
- 将魔法光束、治愈医生、盟友料理、盟友滚轮和画家卡比恢复到对应能力条目；“四个卡比”按画面中的四种卡比猎人职业展开。
- 真实旧数据演练中，411 个旧条目有 409 个明确去向，6524 条历史解锁恢复 6505 条，恢复率 `99.71%`；166 条当前盟友全部恢复。剩余 19 条均为不纳入角色与能力图鉴的传送之星。

### 素材与验证

- 补充素材清单、文件名和 `entry_key` 均为 275 项且无重复；所有图片可正常解码，清单 SHA-256 与本地文件全部一致。
- 增加同页普通能力与 EX 独立解锁、进度和排行榜回归测试，以及“活动数据 + 迁移前备份”二次迁移和历史源只读校验。
- 迁移报告增加数据源标识和 `历史数据合并.csv`，分别显示活动解锁、历史补回和最终有效解锁数。
- 真实 v3.0.1 → v3.1 两阶段演练最终恢复 `6351` 个有效解锁；用户 `2127074778` 在群 `932436510` 为 `200/1353`、群内第 3，唯一漏迁记录仍为“传送之星”。
- README 新增双素材目录上传、dry-run、正式迁移和独立计数说明；运行时注册版本与元数据同步更新为 `v3.1.0`。

## v3.0.1 - 2026-08-04

### 图鉴范围与迁移复核

- 从 1092 个基准来源页面中明确排除 14 个物件、机制、地点、组织、种族或类型汇总页面，最终规范图鉴包含 1078 个角色；排除项及原因单独写入 `已排除非角色页.csv`。
- 为 `Square Kirby` 补录 2019 年 Kirby Portal 愚人节企划首作信息；最终 1078 个条目均有可用于排序的首作年份。
- 真实旧数据重新 dry-run 后，411 个旧条目仍有 407 个明确去向，6524 条历史解锁成功映射 6464 条，恢复率保持 `99.08%`。
- 明确区分历史记录映射与规范角色去重：基准数据映射后共有 4459 个有效唯一解锁；能力形态、别名、重复图和同角色旧造型不会重复计算进度。
- 修正 AstrBot `@register` 中仍显示 `2.10.3` 的版本号，使运行时注册版本与 `metadata.yaml` 一致更新为 `v3.0.1`。

## v3.0.0 - 2026-08-04

### 规范素材与图鉴编号

- 新增离线迁移工具 `migrate_catalog.py`，可用 WiKirby 规范素材整体替换旧活动素材，并同步重写用户当前盟友和历史解锁引用。
- 新图鉴按最早登场年份、同年作品声明顺序、主角优先、条目类型和 WiKirby 页面标题生成稳定编号；图鉴记录保留 `pageid`、页面标题、首作、年份和条目类型。
- 迁移后的排行榜和个人进度按规范角色去重统计，旧别名、重复图片和同角色版本不会重复增加完成度。

### 旧数据恢复

- 匹配流程依次使用人工覆盖、SHA-256、规范名称、感知哈希、官方称号名称、规范角色归并和版本基础角色规则。
- 支持把人工复核过的组合图片一对多展开到画面中的规范角色，并为每个目标保留同一解锁日期；多个旧记录合并时保留最早日期。
- 维护者的 411 条旧图鉴、6524 条历史解锁基线中，407 个旧条目有明确去向，6464 条解锁成功恢复，恢复率为 `99.08%`。
- 仍未迁移的 60 条解锁仅来自“盟友料理”“盟友翻滚”“传送之星”三种非角色素材，全部记录到 `漏迁用户记录.csv`，不进行猜测性分配。

### 迁移安全与报告

- 默认只 dry-run；正式替换必须同时传入 `--apply --confirm REPLACE_OLD_KIRBY_DATA`。
- 新增迁移汇总、旧素材匹配、漏迁用户记录、用户影响、新编号、完整计划和 HTML 图片复核报告。
- 正式执行会先完整构建并校验暂存目录，再原子替换活动目录；旧数据自动保留为 `.before-v3-时间戳` 备份，失败时自动恢复。
- 活动 `img/allies` 只写入新素材，旧素材不会混入新抽取池；未知用户字段、抽取限制和非图鉴插件数据继续保留。
- README 新增公开迁移指南，明确 dry-run、报告复核、正式执行、备份回滚和 AstrBot 重载步骤。

## v2.10.4 - 2026-08-04

### 素材文件名兼容

- 修复作品前缀素材中角色名包含半角点号时被截断的问题；`Kirby Air Riders.J.J.png` 现在会正确解析为作品 `Kirby Air Riders`、角色名 `J.J`。
- 管理员改名时改为复用图鉴记录中的完整作品来源，不再按角色名中的最后一个点号误切来源前缀。
- README 补充批量素材的推荐命名规则、Windows 禁用标点处理方式和带点号角色名示例。
- 增加带点号角色名的素材扫描、解析与改名回归测试。

## v2.10.3 - 2026-08-04

### 修复

- 合并“今日盟友”的命令与纯文本触发入口，修复 `/今日盟友` 同时命中两个 Handler、连续抽取两次的问题。
- `今日盟友`、`抽盟友`、`抽取盟友` 均继续支持带 `/` 和不带 `/` 的写法；一次消息只会执行一次抽取。
- 增加 Handler 注册级回归测试，直接检查六种命令写法共用唯一入口，防止重复触发再次出现。

## v2.10.2 - 2026-08-03

### 完整百科内容

- WiKirby 与 Kirby Fandom 不再按字符预算或条目数量截断内容；简介、正文、信息框、分类、语录、招式及表格行均完整保留。
- WiKirby 搜索命中页面后会重新读取完整页面，避免直接使用搜索阶段的短摘要；MediaWiki 导语请求不再携带 `exchars`。
- Fandom 章节列表不再限制为前 60 项，长页面的全部章节均可查看和查询。
- 修复名称含 `Move`、说明含 `Description` 的正常招式行可能被误判为重复表头的问题。
- 移除三个已失效的长度配置项，以及卡片中的“已显示/未显示”数量提示；旧构造参数仍可传入，但不会裁剪内容。
- 新增长导语、24 项信息框、25 条语录、40 条招式、55 行 WiKirby 表格与完整搜索命中的回归测试。

## v2.10.1 - 2026-08-03

### 修复与优化

- 修复 Kirby Fandom 招式表中的操作说明仍显示英文的问题；常见手柄、方向键、摇杆、冲刺和按住等文字现在会确定性翻译为中文。
- Fandom LLM 翻译现在可以翻译 `controls` 中的自然语言和招式引用，同时由插件校验按键、方向、加号、平台对应关系和换行数量；不安全的结果自动回退原文。
- 招式长度预算按作品和 Type A / Type B 分组公平分配，避免前一个表格占满预算后后续作品或分组完全消失。
- 卡片计数在截断时显示“已显示 x/y 项”，明确区分当前展示数量和页面总数量。
- 新增操作翻译、非法按键变更回退和多作品招式预算的回归测试，并用 Artist 页面完成 T2I 卡片验收。

## v2.10.0 - 2026-08-03

### Fandom 结构化卡片

- `Related Quotes` 不再被压成普通段落：每条语录现在独立保留正文、说明与作品出处，并在卡片中使用全宽引用排版。
- `Techniques` 现在读取网页表格结构，保留作品上下文、Type A / Type B 分组，以及“招式、操作、说明、伤害”四列；手柄按钮图片会转换为可读操作文字。
- `卡比F 页面名 | Techniques`、`卡比F 页面名 | Related Quotes` 和 LLM 百科工具均可返回对应结构化栏目；特殊栏目不会再与普通正文重复显示。
- Fandom 可选翻译改为结构化 JSON 翻译。LLM 只翻译语录、招式名和说明等自然语言，操作、伤害数字、数组顺序与卡片布局由插件固定控制；结果格式异常时完整回退原文。
- 富结构内容继续只以解析后的字段进入缓存，不保存整页 HTML；默认查询按正文长度预算截取，并明确显示未展示项目数。

### 视觉与质量

- 四套百科模板新增协调的语录条和全宽招式表，长说明自动换行，表格保持固定列宽、交替浅色行和顶部对齐。
- 使用 AstrBot T2I 真实渲染 Blade Knight 与 Artist 页面进行验收，输出宽度约 `2880px`，未发现截断、重叠或空白栏目。
- 新增语录解析、嵌套 Tabber 招式表、按钮替代文字、章节命中、结构化翻译回退和卡片 payload 回归测试。

## v2.9.1 - 2026-08-03

### 命令优化

- 新增推荐短命令 `卡比F`、`卡比F名称` 和 `卡比F章节`，指定栏目可使用 `卡比F 页面名 | 栏目名`。
- 保留 `卡比Fandom`、`卡比Fandom名称`、`卡比Fandom章节` 和 `卡比社区百科` 作为兼容别名，不影响已有使用习惯。
- 候选页面提示、章节提示、群内帮助和 README 统一优先展示短命令。

## v2.9.0 - 2026-08-03

### 新增

- 新增 `我的图鉴进度`，显示用户当前有效解锁数、图鉴总数、完成率、进度条和剩余数量；旧名称别名只计一次，不会虚增进度。
- 新增 Kirby Wiki | Fandom 查询：支持页面简介、便携信息框、分类、正文栏目、首图、日文名和跨语言社区页面名称。
- 新增 `卡比Fandom`、`卡比Fandom名称`、`卡比Fandom章节`，以及 `卡比Fandom 页面名 | 栏目名` 精确栏目查询；同时支持 `卡比社区百科` 别名和可选 `/` 前缀。
- 新增 LLM 工具 `kirby_catalog_lookup_fandom` 与 `kirby_catalog_lookup_fandom_names`，让 AstrBot 模型可以主动查询 Fandom 页面资料和跨语言社区页面名称。
- Fandom 查询独立提供回复形式、卡片模板、首图、详情、缓存、正文长度和 AstrBot 原生 LLM 翻译设置。

### 卡片与优化

- 四套百科卡片改为更浅、更协调且符合主题名称的配色；资料、多语言名称和正文栏目使用不同但协调的色块，不再大面积使用单一白底或深色背景。
- 将 AstrBot T2I 逻辑视口提升到 `1600px`，保留设备像素截图与 `ultra` 清晰度；实测长卡片输出宽度约 `2880px`，正文也同步增大以改善群聊缩放后的可读性。
- 个人图鉴标题改为显示有效的 `已解锁 x/总数`，失效旧名不会继续占用进度。
- Fandom 页面缓存只保留解析后的可读字段，不再常驻整页 HTML 或 Wikitext，降低大量查询后的内存占用。
- 补全插件帮助、公开文档、版本元数据和请求 User-Agent。

## v2.8.3 - 2026-08-03

### 卡片渲染与视觉优化

- 修复百科卡片把嵌套 `viewport` 参数传给 AstrBot T2I 服务的问题。现在明确使用 `1280px` 画布、设备像素截图和 `ultra` 清晰度等级生成 PNG，避免服务端悄悄回退到默认 `800px` 截图。
- 重做“梦之泉”“卡比粉彩”“瓦豆鲁迪”“星际档案”四套模板：正文、资料速览、首图和来源改为高对比连续色带，不再使用大面积白色底；四套模板也分别采用不同的标题和首图排版。

## v2.8.2 - 2026-08-03

### 修复与新增
- `卡比百科名称` 现在会在 `{{Names}}` 模板缺失时，识别 `Names in other languages` 栏目中手写的 `Language / Name` 表格；多个角色形态或称号表会保留各自标题，避免混淆。
- 新增 AstrBot LLM 工具 `kirby_catalog_lookup_wikirby`，让模型可以主动查询 WiKirby 的简介、页面资料、其他语言名称与来源链接；工具只读，不会改变图鉴数据或发送群消息。
- LLM 工具查询保持原始百科资料，不会在工具调用过程中再发起一次翻译模型请求。

## v2.8.1 - 2026-08-03

### 修复与优化
- 页面详情现在始终优先读取 WiKirby 渲染后的 HTML；没有 `Locations` 栏目的页面也能取得完整正文、层级标题和可读表格，不会把旧版 MediaWiki 表格、缩略图或文件语法带进卡片。
- 重排单张百科卡片：资料速览和其他语言名称改为正文前的横向资料带，后续页面内容使用全宽双栏，消除长卡片后半段的整列空白。
- 四套卡片模板统一使用中文档案标签，并保留页面英文标题、来源和主题名，方便群聊直接阅读。
- 修复窄屏渲染时摘要与首图仍按桌面双列挤压的问题；现在会自动纵向排布，不会把标题和正文压成窄列。

## v2.8.0 - 2026-08-03

### 修复
- 修复“仅百科卡片”固定使用窄幅单栏布局，导致右侧空间浪费、整张图片过长且文字难以阅读的问题。
- 卡片改为自适应宽幅画布；简介和首图放入顶部主视觉，资料速览放入侧栏，正文栏目使用双栏编排。
- 兼容支持或忽略 `selector` 选项的 HTML 渲染端：支持时精确裁切，不支持时使用完整页面截图，避免内容被固定视口截断。

### 新增
- 增加“梦之泉”“卡比粉彩”“瓦豆鲁迪”“星际档案”四套可选 HTML 卡片模板。
- 卡片始终只生成一张完整图片，保持正文、资料和来源在同一张百科卡片中。
- 卡片模板全部使用内嵌 HTML/CSS 和已下载的页面首图，不依赖外部网页资源。

## v2.7.0 - 2026-08-03

### 修复
- 合并 `卡比百科` 的命令和纯文本触发入口，避免 `/卡比百科 <角色>` 同时命中两个 Handler 而重复发送两条百科。
- 明确 WiKirby 的 403 fallback 只负责请求重试和数据回退，不会产生额外回复。

### 新增
- 普通百科内容加入 `Names in other languages`，显示页面记录的多语言官方名称和罗马化读法。
- 增加百科回复形式配置：普通消息、合并转发、仅百科卡片、百科文字+卡片、文字+卡片合并转发。
- 卡片模式使用 AstrBot 原生 T2I 服务渲染，支持把已获取的页面首图嵌入卡片；渲染失败自动回退。

## v2.6.1 - 2026-08-03

### 修复与优化
- 补全 `卡比百科` 的出现地点：读取 WiKirby 渲染后的 Locations 表，只显示页面标记为出现的实际关卡。
- 保持 Gallery、References、Names in other languages 和导航框不直接输出，避免百科回复被图片文件名、参考链接和重复内容刷屏。
- 统一插件注册版本、请求 User-Agent 和公开元数据。

## v2.6.0 - 2026-08-03

### 修复与优化

- 移除百科回复中的剧透提示。
- 不再只读取页面简介；增加信息框、出现地点和 Trivia 趣闻等可读页面资料，跳过 Gallery、参考文献和语言名称表。
- 将“卡比百科名称”注册为 AstrBot 原生 LLM 工具 `kirby_catalog_lookup_official_names`，让 Bot 可以主动查询官方名称。
- 增加详细页面资料开关。

### 新增

- 增加可选的 WiKirby 简介和页面资料 AI 翻译，使用 AstrBot 原生文本模型，不需要在插件中填写额外的 API Key。
- 翻译失败时自动回退到 WiKirby 原文，不影响正常查询。

## v2.5.0 - 2026-08-03

### 修复与优化

- 修复带 `/` 或唤醒前缀的 `卡比百科` 可能同时命令触发和纯文本触发，导致同一条百科信息发送两次的问题。

## v2.4.1 - 2026-08-03

### 修复与优化

- 修复百科文字已经通过 Worker，但 AstrBot 发送图片时仍直连 CDN 导致 403 的问题。
- 图片下载现在也使用 Worker Bearer 密钥，并在图片不可用时保留文字回复。
- Worker 增加受限的 WiKirby CDN 图片转发路径。

## v2.4.0 - 2026-08-03

### 新增

- 增加可选 Cloudflare Worker 中转支持，适用于云服务器出口 IP 被 WiKirby 拦截的情况。
- 提供受限路径、Bearer 密钥和缓存配置的 Worker 示例。

## v2.3.1 - 2026-08-03

### 修复与优化

- 增加静态 `action=raw` 页面兜底，处理云服务器同时被 API 和 REST 接口拦截的情况。
- 精确查询角色时，即使动态接口不可用，也能继续读取简介、名称表和 CDN 图片。

## v2.3.0 - 2026-08-03

### 修复与优化

- 针对云服务器出口 IP 被 WiKirby `api.php` 拒绝的情况，增加 REST API 兜底。
- 页面查询、中文搜索和多语言名称读取均可在 MediaWiki API 不可用时继续工作。
- 限制 MediaWiki 摘要参数不超过站点允许的 1200 字符，消除接口警告。

## v2.2.1 - 2026-08-03

### 修复与优化

- 处理 WiKirby 对部分服务器出口间歇性返回 HTTP 403 的情况。
- 增加短暂重试、备用 `www.wikirby.com` API 地址和明确的请求标识。
- WiKirby 暂时不可用时仍不会影响抽盟友、猜盟友和图鉴功能。

## v2.2.0 - 2026-08-03

### 新增

- 增加 `卡比百科`，可查询 WiKirby 页面摘要、首图和来源链接。
- 增加 `卡比百科名称`，专门提取角色在各语言中的官方名称和罗马化读法。
- 支持中文名称搜索、英文页面名搜索、重定向和多候选页面提示。
- 支持引用图鉴盟友消息后直接查询对应的 WiKirby 页面。

### 修复与优化

- WiKirby 查询使用公开 MediaWiki API、短期缓存和并发限制，不影响抽盟友与图鉴数据。
- 查询失败、页面不存在或候选不明确时给出明确提示，不会自动修改图鉴名称。

## v2.1.3 - 2026-08-03

### 修复与优化

- 猜盟友答对不再自动写入个人图鉴。
- 停止启动时按同名自动合并素材，避免把正常的同名条目从全图鉴中隐藏。
- 恢复仍存在于素材目录、但被旧版本错误标记为同名别名的条目，并补回缺失编号。

## v2.1.2 - 2026-08-03

### 新增

- 增加管理员按“重复编号 正确编号”明确合并重复条目的命令，并同步用户记录。

## v2.1.1 - 2026-08-03

### 修复与优化

- 修复旧版本改名后，旧素材文件或旧目录记录再次被扫描为重复盟友的问题。
- 合并同一来源和名称的重复图鉴条目，并将用户记录迁移到保留条目。
- 增加管理员一次性清理改名前缀的命令，支持保留指定的旧名称。

## v2.1.0 - 2026-08-03

### 新增

- 猜盟友时支持引用题目图片并直接发送名字作答。
- 猜盟友进行中禁止其他用户重新发起新题目，避免覆盖当前题目。
- 增加 `随机盟友` 命令，只展示随机盟友，不记录用户抽取、当前盟友或解锁数据。
- 随机盟友消息同样包含图鉴编号，可直接引用后使用改名命令。
- 自动修复旧图鉴中重复的编号，并避免改名后的旧兼容素材被重新加入图鉴。
- 改名时同步修改素材文件名，保留如 `星之卡比 探索发现.` 这样的来源前缀。

## v2.0.2 - 2026-08-03

### 新增

- 管理员引用 Bot 发出的盟友消息后，可以只填写新名字；插件会自动识别引用消息中的图鉴编号，并同步所有相关数据。

## v2.0.1 - 2026-08-03

### 修复与优化

- 猜盟友答错后立即公布正确答案并结束本轮，不再允许继续盲猜。
- 猜盟友超时后由插件主动发送正确答案，不必等待下一条消息。
- 支持直接发送“今日盟友”触发抽取，不要求使用 `/` 或其他命令前缀。

## v2.0.0 - 2026-08-02

### 新增

- 更名为“星之卡比图鉴”，使用独立插件标识 `astrbot_plugin_kirby_catalog`。
- 增加带固定编号和名字的群图鉴、个人图鉴与盟友名单查询。
- 增加猜盟友功能，答对后自动解锁。
- 增加管理员手动添加盟友、引用图片替换素材和改名命令。
- 增加群内盟友解锁排行榜。
- 增加 `requirements.txt`、配置 schema、MIT License 和数据迁移说明。

### 数据与兼容性

- 迁移旧插件群组配置、个人解锁记录、每日抽取次数和图鉴编号。
- 迁移旧插件本地图片到新目录 `img/allies`，新插件可独立运行。
- 改名时同步所有用户的当前盟友、历史解锁记录和后续图鉴。
- 使用原子 JSON 写入，降低并发抽取导致数据损坏的风险。
- 移除原插件中与收藏图鉴无关的夺取相关功能及状态读取。

### 修复与优化

- 修复旧图鉴编号在刷新素材目录时可能被重新分配的问题。
- 修复图鉴缓存不更新、素材改名后历史记录失联、旧配置数组格式兼容不完整等问题。
- 图鉴生成放到线程中执行，避免 Pillow 图片处理阻塞 AstrBot 事件循环。
